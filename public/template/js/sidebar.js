// Lấy tất cả các mục menu có dropdown
const dropdownMenus = document.querySelectorAll('.has-dropdown');

// Duyệt qua từng mục menu có dropdown
dropdownMenus.forEach((dropdownMenu) => {
  // Lấy dropdown menu của mục menu hiện tại
  const submenu = dropdownMenu.querySelector('.submenu');

  // Thiết lập sự kiện click cho mục menu hiện
})